@model KPaTS.Models.LocalPasswordModel
@{
    ViewBag.Title = "Управление учетной записью";
}

<div class="mdl-grid">
<p class="message-success">@ViewBag.StatusMessage</p>

<section id="loginForm" class="mdl-color--white mdl-cell mdl-cell--8-col card-padding">
@if (ViewBag.HasLocalPassword)
{
    @Html.Partial("_ChangePasswordPartial")
}
else
{ 
    @Html.Partial("_SetPasswordPartial")
}
</section>

<section class="social mdl-shadow--2dp mdl-color--white mdl-cell mdl-cell--4-col card-padding" id="externalLogins">
    @Html.Action("RemoveExternalLogins")
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
